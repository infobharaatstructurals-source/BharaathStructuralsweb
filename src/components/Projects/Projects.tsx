import Icon from '../common/Icon'
import { projects } from '../../data/siteData'
import './Projects.css'
export default function Projects({onNavigate}:{onNavigate:(id:string)=>void}){return <section id="projects" className="projects-section section-light"><div className="section-wrap"><div className="section-heading" data-reveal><div><div className="eyebrow blue">SELECTED WORK</div><h2 className="display-title">Projects in<br/><span>detail.</span></h2></div><button className="outline-btn" onClick={()=>onNavigate('contact')}>Start a project <Icon name="ArrowUpRight" size={15}/></button></div><div className="projects-scroller">{projects.map((project,i)=><article className="project-card" key={project.title} data-reveal><div className="project-image" style={{backgroundImage:`url(${project.image})`}}><span className="project-no">0{i+1}</span><span className="project-open"><Icon name="ArrowUpRight" size={21}/></span></div><div className="project-info"><div><span>{project.category}</span><h3>{project.title}</h3><p>{project.meta}</p></div><Icon name="ArrowRight" size={19}/></div></article>)}</div></div></section>}
